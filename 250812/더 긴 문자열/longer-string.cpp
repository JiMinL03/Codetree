#include <iostream>
#include <string>
#include <cstring>
using namespace std;

int main() {
    string str1, str2;   
    cin >> str1 >> str2;
    int len1 = strlen(str1);
    int len2 = strlen(str2);

    if(len1 > len) cout << str1 << " " << len1;
    else cout << str2 << " " << len2;
    return 0;
}